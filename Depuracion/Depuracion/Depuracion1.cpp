#include <stdio.h>
#include <stdlib.h>

int potencia(int base, int exponente)
{
	int i, pot=0;
	for(i=0; i<=expoente; i++)
		pot *=base;
	return pot;
}

int main()
{
	printf("5 a la 3 es igual a: %d\n" potencia(5,3) );
	system("pause");
	return 0;
}